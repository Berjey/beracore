node_modules/
# yerel gelistirme veritabani (uretimde /var/www/beracore-data altinda)
.data/
.next/
# deploy sirasinda kullanilan gecici build dizinleri (scripts/server-deploy.sh)
.next-build/
.next-eski/
out/
.DS_Store
*.log
.env
.env.*
!.env.example

# Gecici / karalama dosyalari.
# 2 Agu 2026: `uretim-kimlik.tmp` canli panel parolasini ve AUTH_SECRET'i tasiyordu ve
# HERKESE ACIK depoya commit'lenmisti (e4e00a6). `.env*` kaliplari onu kapsamiyordu.
# Gecmis `git filter-repo` ile temizlendi, kimlikler rotasyona sokuldu.
# Ikinci savunma hatti: `scripts/secret-scan.mjs` (deploy.mjs push'tan once calistirir).
*.tmp
*.bak
*.orig
uretim-kimlik*
kimlik-*.env
secrets.*

# IDE / lokal araç dosyaları
.claude/
.vscode/
tsconfig.tsbuildinfo
